---
description: Show recent lane switch events
---

If `$1` is a positive integer, use the `contexts-events` tool with `limit` set to `$1`.
Otherwise call `contexts-events` with default arguments.
Return only the tool output.
