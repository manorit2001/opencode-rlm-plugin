---
description: Show lane routing token efficiency stats
---

If `$1` is a positive integer, use the `contexts-efficiency` tool with `switchWindow` set to `$1`.
Otherwise call `contexts-efficiency` with default arguments.
Return only the tool output.
